# Terraform
terraform.tfvars
.terraform/
terraform.tfstate
terraform.tfstate.backup
.terraform.tfstate.lock.info

# Fetched binaries
install/oc
install/openshift-install

# Temp dirs and files generated by installer
install/temp/
kubeconfig
kubeadmin-password
